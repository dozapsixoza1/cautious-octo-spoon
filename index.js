// Запускает только бота (админ-панель теперь встроена в бота кнопками — см. src/handlers/admin.js).
// Старая веб-панель (admin-panel/) оставлена в проекте на будущее — если перейдёте на тариф
// с открытым портом, её можно запустить отдельно командой `npm run admin`.
require('./src/bot');
